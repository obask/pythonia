---
slug: lists-and-loops
title: Списки и циклы
description: Пройди по коллекции значений и собери новый ответ шаг за шагом.
moduleId: collections
moduleTitle: Коллекции данных
moduleDescription: Списки, индексы, циклы и словари
moduleOrder: 2
level: Начальный
duration: 20 мин
order: 5
xp: 130
tags:
  - списки
  - циклы
objectives:
  - Перебирать элементы списка
  - Накопить сумму в переменной
  - Отбирать значения с помощью if
quiz:
  - id: q1
    type: multiple-choice
    prompt: Что делает строка for item in numbers?
    options:
      - По очереди берет каждый элемент из numbers
      - Создает словарь с именем item
      - Сортирует numbers в обратном порядке
      - Останавливает программу, если numbers пустой
    answer: По очереди берет каждый элемент из numbers
    explanation: Цикл for временно присваивает переменной item каждый элемент списка.
  - id: q2
    type: short-answer
    prompt: Какой оператор помогает проверить, делится ли число на 2 без остатка?
    answer:
      - "%"
      - modulo
      - остаток
    explanation: Оператор % возвращает остаток от деления. У четных чисел остаток при делении на 2 равен 0.
challenge:
  prompt: Реализуй sum_even(numbers). Верни сумму только четных чисел из списка.
  starterCode: |
    def sum_even(numbers):
        total = 0
        # Добавь к total только четные числа.
        return total
  tests:
    - name: суммирует смешанный список
      code: |
        expect_equal(sum_even([1, 2, 3, 4, 5, 6]), 12)
    - name: возвращает ноль, если четных чисел нет
      code: |
        expect_equal(sum_even([1, 3, 5, 7]), 0)
    - name: работает с отрицательными четными числами
      code: |
        expect_equal(sum_even([-4, -3, 2, 9]), -2)
---

## Списки хранят несколько значений

Список - это упорядоченная коллекция. В нем можно хранить числа, строки, логические значения и даже другие списки.

```python
numbers = [4, 8, 15, 16, 23, 42]
```

Когда ты работаешь со списком, чаще всего первый вопрос такой: что нужно сделать с каждым элементом?

## Цикл for

Цикл `for` нужен, когда ты хочешь пройти по всем элементам.

```python
total = 0
for number in numbers:
    total = total + number
```

Переменная `number` меняется на каждом шаге: сначала это `4`, потом `8`, потом `15` и так далее.

## Фильтрация через if

Не всегда нужны все значения. Условие помогает выбрать только подходящие.

```python
if number % 2 == 0:
    total = total + number
```

`number % 2` возвращает остаток от деления на 2. Если остаток равен 0, число четное.
